#include <iostream>
#include <functional>

using namespace std;

function<int(int)> multiplier(int n)
{
    return [n](int x)
    {
        return x * n;
    };
}

int main()
{
    function<int(int)> times3 = multiplier(3);

    cout << times3(5) << endl;

    return 0;
}
